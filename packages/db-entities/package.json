{
  "name": "@orpheus/db-entities",
  "version": "1.0.0",
  "description": "Contains the shared db entity models",
  "main": "dist/src/index.js",
  "author": "Soumil Singh",
  "type": "module",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:alias": "tsc-alias -p tsconfig.json",
    "clean": "rm -rf dist",
    "build:dts": "tsc --emitDeclarationOnly",
    "build:all": "yarn clean && yarn build && yarn build:alias && rollup -c rollup.config.mjs &&  find ./dist/src -type f -name '*.d.ts' -delete"
  }
}
