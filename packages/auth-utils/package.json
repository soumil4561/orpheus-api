{
  "name": "@orpheus/auth-utils",
  "version": "1.0.0",
  "description": "Orpheus Auth utils",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:alias": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "author": "Soumil Singh",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  }
}
